plugins {
    id 'java'
}

group = 'horry'
version = '1.1.0'

repositories {
    mavenCentral()
    maven { url 'https://jitpack.io' }
    maven { url 'https://raw.githubusercontent.com/Anuken/MindustryMaven/master/' }
}

dependencies {
    compileOnly "com.github.Anuken.Arc:arc-core:master-SNAPSHOT"
    compileOnly "com.github.Anuken.Mindustry:core:master-SNAPSHOT"
    compileOnly "com.github.Anuken.Mindustry:desktop:master-SNAPSHOT"
}

jar {
    archiveFileName = "ip_switch.jar"
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    manifest {
        attributes 'Main-Class': 'horry.ipswitch.IpSwitchMod'
    }
}

java {
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
}
